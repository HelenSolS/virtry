# Virtual Try-On - Виртуальная примерочная

## Обзор проекта

Веб-приложение для виртуальной примерки одежды с использованием AI (Gemini 2.0 Flash / Nano Banana).

### Возможности
- ✨ Загрузка фото человека и одежды
- 🤖 AI-генерация изображения с примеркой
- 🎨 Современный адаптивный интерфейс
- ⚡ Быстрая обработка на Cloudflare Workers

## Текущие функции

✅ **Реализовано:**
- Адаптивный UI с hero-секцией и прокруткой
- Загрузка двух изображений (человек + одежда)
- Drag & Drop для загрузки файлов
- API endpoint для обработки изображений
- Интеграция с Google Gemini 2.0 Flash
- Двухэтапная обработка (описание одежды → генерация)

❌ **Не реализовано:**
- Hero видео (требуется замена заглушки на реальное видео)
- Продакшн деплой на Cloudflare Pages
- История примерок
- Сохранение результатов

## Точки входа API

### GET /
Главная страница приложения

### POST /api/tryon
Генерация виртуальной примерки

**Параметры (FormData):**
- `photo` (File) - фото человека
- `outfit` (File) - фото одежды

**Ответ:**
```json
{
  "success": true,
  "image": "data:image/png;base64,..."
}
```

**Ошибки:**
```json
{
  "error": "описание ошибки",
  "details": "детали ошибки"
}
```

## Архитектура данных

### Модели данных
- **Input**: Два изображения (photo, outfit) в формате File/Blob
- **Processing**: Конвертация в base64 для API
- **Output**: base64-encoded изображение результата

### Сервисы хранения
- **Cloudflare Workers**: Edge runtime для API
- **Google Gemini API**: AI-генерация изображений
- Нет персистентного хранилища (stateless)

### Поток данных
1. Пользователь загружает 2 изображения через UI
2. Frontend отправляет FormData на `/api/tryon`
3. Backend конвертирует изображения в base64
4. Шаг 1: API описывает одежду через Gemini
5. Шаг 2: API генерирует итоговое изображение
6. Результат возвращается как base64 data URL

## Руководство пользователя

### Как использовать:

1. **Откройте приложение** - при загрузке увидите hero-секцию
2. **Прокрутите вниз** - нажмите на стрелку или прокрутите страницу
3. **Загрузите свое фото** - нажмите на первую область или перетащите изображение
4. **Загрузите фото одежды** - аналогично для второй области
5. **Нажмите "Создать образ"** - дождитесь обработки (10-30 секунд)
6. **Просмотрите результат** - изображение появится справа

### Требования к изображениям:
- Форматы: JPG, PNG, WEBP
- Фото человека: в полный рост, хорошее освещение
- Фото одежды: четкое изображение предмета одежды

## Настройка и запуск

### Локальная разработка:

```bash
# 1. Клонировать репозиторий
cd /home/user/webapp

# 2. Установить зависимости (уже установлены)
npm install

# 3. Настроить API ключ
cp .dev.vars.example .dev.vars
# Отредактируйте .dev.vars и добавьте ваш GOOGLE_API_KEY

# 4. Собрать проект
npm run build

# 5. Запустить через PM2
fuser -k 3000/tcp 2>/dev/null || true
pm2 start ecosystem.config.cjs

# 6. Открыть в браузере
# http://localhost:3000
```

### Получение Google API ключа:

1. Перейдите на https://aistudio.google.com/apikey
2. Создайте новый API ключ
3. Скопируйте ключ в `.dev.vars`:
   ```
   GOOGLE_API_KEY=your_actual_key_here
   ```

### Деплой на Cloudflare Pages:

```bash
# 1. Установить API ключ (выполняется один раз через AI)
# Вызывается setup_cloudflare_api_key через AI

# 2. Собрать проект
npm run build

# 3. Создать Pages проект
npx wrangler pages project create webapp --production-branch main

# 4. Задеплоить
npx wrangler pages deploy dist --project-name webapp

# 5. Установить секреты
npx wrangler pages secret put GOOGLE_API_KEY --project-name webapp
# Введите ваш Google API ключ
```

## Деплой

- **Платформа**: Cloudflare Pages
- **Статус**: ⚠️ Не задеплоено (требуется API ключ)
- **Технологии**: 
  - Hono (Backend framework)
  - TypeScript
  - Vanilla JS (Frontend)
  - Google Gemini 2.0 Flash API
  - Cloudflare Workers
- **Последнее обновление**: 2026-02-04

## Рекомендуемые следующие шаги

1. **Добавить реальное hero видео** 
   - Заменить заглушку на видео моды/одежды
   - Можно использовать видео от Sora или бесплатные источники

2. **Настроить Google API ключ**
   - Получить ключ на https://aistudio.google.com/apikey
   - Добавить в `.dev.vars` для разработки
   - Установить через wrangler secrets для продакшена

3. **Задеплоить на Cloudflare Pages**
   - Запустить `npm run deploy:prod`
   - Настроить custom domain (опционально)

4. **Улучшить UI/UX**
   - Добавить preview результатов
   - Галерея примеров
   - Возможность скачать результат

5. **Оптимизация**
   - Кэширование результатов
   - Сжатие изображений перед отправкой
   - Rate limiting для API

6. **Дополнительные функции**
   - Поддержка множественных предметов одежды
   - Выбор стиля/настроения
   - История примерок (с D1 или KV)

## Структура проекта

```
webapp/
├── src/
│   ├── index.tsx          # Основной файл приложения (Hono)
│   └── renderer.tsx       # JSX renderer для HTML
├── public/
│   └── static/
│       ├── style.css      # Стили приложения
│       ├── app.js         # Frontend JavaScript
│       └── hero-video.mp4 # Hero видео (заглушка)
├── .dev.vars.example      # Пример переменных окружения
├── .gitignore             # Git ignore файл
├── ecosystem.config.cjs   # PM2 конфигурация
├── wrangler.jsonc         # Cloudflare конфигурация
├── package.json           # Dependencies и scripts
└── README.md              # Этот файл
```

## Технические детали

### API Endpoint Implementation

Используется двухэтапный подход для лучших результатов:

1. **Шаг 1 - Описание одежды:**
   - Модель анализирует изображение одежды
   - Извлекает: тип, стиль, цвета, паттерны, детали
   - Возвращает структурированное описание

2. **Шаг 2 - Генерация:**
   - Получает оба изображения + описание одежды
   - Создает детальный prompt с инструкциями
   - Генерирует финальное изображение

### Prompt Engineering

Ключевые элементы промпта:
- Явная маркировка изображений (PHOTO A, PHOTO B)
- Четкие требования к сохранению (лицо, поза, фон)
- Описание одежды из первого шага
- Требования к реалистичности

## Известные проблемы

1. **Видео не отображается** - требуется реальный video файл
2. **Качество примерки** - зависит от качества входных изображений
3. **Время обработки** - 10-30 секунд на генерацию
4. **Лимиты API** - Google Gemini имеет rate limits

## Лицензия

MIT

## Контакты

При возникновении проблем создайте Issue в репозитории.
