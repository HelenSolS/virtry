# 🎯 Production Workflow: Sandbox → GitHub → Vercel

## Правильная архитектура для вас

```
┌─────────────────────────────────────────────────────────────┐
│ 1. Разработка и тестирование (Sandbox)                      │
│    - Работаете здесь с AI Developer                         │
│    - Тестируете на http://localhost:3000                    │
│    - Проверяете всё работает                                │
└────────────────────┬────────────────────────────────────────┘
                     │
                     │ git push (любые коммиты)
                     ▼
┌─────────────────────────────────────────────────────────────┐
│ 2. GitHub (хранилище кода + история)                        │
│    - Все коммиты сохраняются                                │
│    - История изменений                                       │
│    - Backup кода                                             │
│    - НЕ деплоится автоматически на Vercel                   │
└────────────────────┬────────────────────────────────────────┘
                     │
                     │ ТОЛЬКО когда готово к Production
                     │ Manual Deploy (через Vercel Dashboard)
                     ▼
┌─────────────────────────────────────────────────────────────┐
│ 3. Vercel Production (только финальные версии)              │
│    - https://virtry.vercel.app                              │
│    - Только утвержденные релизы                             │
│    - Ручной деплой по вашему решению                        │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔧 Настройка (ВАЖНО!)

### Вариант A: Manual Deploy только ⭐ РЕКОМЕНДУЮ ДЛЯ ВАС

**Этот вариант позволяет:**
- ✅ Работать здесь в Sandbox
- ✅ Пушить в GitHub любые коммиты (тестовые, WIP, etc.)
- ✅ GitHub НЕ деплоит автоматически на Vercel
- ✅ Вы вручную деплоите только финальные версии

#### Настройка:

1. **Создайте проект на Vercel БЕЗ GitHub integration**:
   ```
   https://vercel.com/dashboard
   → Add New → Project
   → НЕ ВЫБИРАЙТЕ "Import Git Repository"
   → Вместо этого используйте CLI
   ```

2. **Используйте Vercel CLI для ручного деплоя**:
   ```bash
   # Когда готовы задеплоить финальную версию:
   cd /home/user/webapp
   vercel --prod
   ```

3. **Workflow**:
   ```bash
   # 1. Работаете здесь в Sandbox
   # Делаете изменения, тестируете
   
   # 2. Коммитите в GitHub (любые коммиты)
   git add .
   git commit -m "WIP: testing new feature"
   git push origin main
   # ☑️ GitHub обновлён, но Vercel НЕ деплоит
   
   # 3. Когда всё готово и протестировано
   git commit -m "Release v1.0.1: Ready for production"
   git push origin main
   
   # 4. ВРУЧНУЮ деплоите на Vercel
   vercel --prod
   # ✅ Только сейчас обновляется Production
   ```

**Преимущества**:
- ✅ Полный контроль над Production деплоями
- ✅ GitHub используется для всех коммитов
- ✅ Vercel обновляется только когда вы решите

---

### Вариант B: Protected Production Branch

**Настройка "умного" автодеплоя:**

```
main branch (development)
  ↓ git push (любые коммиты)
GitHub (сохраняет всё)
  ↓ НЕ деплоит на Vercel
  
production branch (только финальные версии)
  ↓ git merge main (когда готово)
  ↓ git push origin production
Vercel (автодеплой ТОЛЬКО с production branch)
```

#### Настройка:

1. **Создайте отдельную ветку для Production**:
   ```bash
   cd /home/user/webapp
   git checkout -b production
   git push origin production
   ```

2. **Настройте Vercel на деплой ТОЛЬКО с production branch**:
   ```
   Vercel Dashboard → Settings → Git
   → Production Branch: production
   → Ignored Build Step: (пусто - деплоить всегда с production)
   ```

3. **Workflow**:
   ```bash
   # 1. Обычная работа в main
   git checkout main
   git add .
   git commit -m "Testing new feature"
   git push origin main
   # ☑️ Только GitHub обновлён, Vercel НЕ трогает
   
   # 2. Тестируете здесь в Sandbox
   npm run build
   pm2 restart webapp
   # Проверяете http://localhost:3000
   
   # 3. Когда всё готово - мердж в production
   git checkout production
   git merge main
   git push origin production
   # ✅ Теперь Vercel автоматически деплоит
   ```

**Преимущества**:
- ✅ Чёткое разделение: main = dev, production = prod
- ✅ Автодеплой только финальных версий
- ✅ История в обеих ветках

---

### Вариант C: Git Tags для релизов

**Используйте теги для финальных версий:**

```
main branch (любые коммиты)
  ↓ git push
GitHub (сохраняет всё)
  ↓ НЕ деплоит
  
git tag v1.0.0 (финальная версия)
  ↓ git push --tags
Vercel (деплоит ТОЛЬКО tagged версии)
```

#### Настройка:

1. **Настройте Vercel на деплой только с тегами**:
   ```
   Vercel Dashboard → Settings → Git
   → Production Branch: main
   → Ignored Build Step:
   [[ "$(git describe --exact-match --tags HEAD 2>/dev/null)" == "" ]]
   ```
   (деплоит только если есть тег)

2. **Workflow**:
   ```bash
   # 1. Обычная работа
   git add .
   git commit -m "Add feature"
   git push origin main
   # ☑️ GitHub обновлён, Vercel НЕ деплоит
   
   # 2. Когда готово к релизу
   git tag -a v1.0.0 -m "Release v1.0.0"
   git push origin main --tags
   # ✅ Vercel видит тег и деплоит
   ```

---

## 📊 Сравнение вариантов

| Вариант | Контроль | Сложность | Автодеплой | Рекомендация |
|---------|----------|-----------|------------|--------------|
| **A: Manual** | 🟢 Полный | 🟢 Простой | ❌ Нет | ⭐ **Для вас** |
| **B: Protected** | 🟡 Средний | 🟡 Средний | ✅ Да | Для команды |
| **C: Tags** | 🟢 Полный | 🔴 Сложный | ✅ Да | Для опытных |

---

## 🎯 Рекомендация для вас: Вариант A (Manual Deploy)

### Почему:

1. **Вы работаете один** - не нужна сложная система
2. **Хотите полный контроль** - деплоите когда решите
3. **Простота** - нет сложных веток и настроек
4. **Безопасность** - случайно не задеплоите незавершённый код

### Как это работает:

```bash
# === ЕЖЕДНЕВНАЯ РАБОТА ===

# 1. Работаете здесь в Sandbox с AI Developer
# Делаете изменения, тестируете локально

# 2. Коммитите и пушите в GitHub (ЛЮБЫЕ коммиты)
git add .
git commit -m "WIP: working on new feature"
git push origin main
# → GitHub обновлён
# → Vercel НЕ трогает Production

# 3. Продолжаете работать, ещё коммиты
git commit -m "Fix bug"
git push origin main
# → Снова только GitHub

# 4. Тестируете здесь
npm run build
pm2 restart webapp
curl http://localhost:3000
# → Всё работает!

# === КОГДА ГОТОВО К PRODUCTION ===

# 5. Финальный коммит
git add .
git commit -m "Release v1.0.1: Add new feature X"
git push origin main

# 6. РУЧНОЙ деплой на Vercel (ТОЛЬКО КОГДА ГОТОВО)
vercel --prod
# → Vercel деплоит на Production
# → https://virtry.vercel.app обновлён

# 7. Проверяете Production
curl https://virtry.vercel.app
# → Всё работает!
```

---

## 🛠️ Настройка Manual Deploy

### Шаг 1: Установите Vercel CLI (если ещё не установлен)

```bash
npm install -g vercel
```

### Шаг 2: Войдите в аккаунт

```bash
vercel login
```

### Шаг 3: Первый деплой (ОДИН РАЗ)

```bash
cd /home/user/webapp
vercel
```

Vercel спросит:
- **Set up and deploy?** → Yes
- **Which scope?** → Ваш аккаунт
- **Link to existing project?** → No
- **Project name?** → virtry (или другое)
- **Directory?** → ./ (оставить по умолчанию)
- **Override settings?** → No

### Шаг 4: Настройте Environment Variables (ОДИН РАЗ)

```bash
# Добавьте GATEWAY_URL
vercel env add GATEWAY_URL production
# Вставьте: https://gateway.ai.cloudflare.com/v1/{account_id}/...

# Добавьте GATEWAY_TOKEN
vercel env add GATEWAY_TOKEN production
# Вставьте: cf_xxxxxxxxxxxxx
```

### Шаг 5: Production Deploy

```bash
vercel --prod
```

**Готово!** Ваш сайт на Production!

---

## 📝 Ежедневные команды

### Разработка здесь в Sandbox:

```bash
# Работаете с кодом
nano src/index.tsx

# Билд и тест
npm run build
pm2 restart webapp

# Проверяете
curl http://localhost:3000

# Коммитите (любые изменения)
git add .
git commit -m "Update feature"
git push origin main
```

### Деплой на Production (когда готово):

```bash
# Проверьте что всё готово
git status
git log --oneline -5

# Деплой
vercel --prod

# Проверьте Production
curl https://virtry.vercel.app
```

---

## 🔍 Проверка статуса

### Посмотреть список деплоев:

```bash
vercel ls
```

### Посмотреть логи последнего деплоя:

```bash
vercel logs
```

### Посмотреть информацию о проекте:

```bash
vercel inspect
```

---

## 🚫 Как НЕ настраивать автодеплой

Если вы случайно подключили GitHub к Vercel и он деплоит автоматически, отключите:

### Вариант 1: Удалите Git Integration

1. Откройте https://vercel.com/dashboard
2. Выберите проект
3. **Settings** → **Git**
4. **Disconnect** (отключить GitHub)

### Вариант 2: Используйте Ignored Build Step

1. **Settings** → **Git**
2. **Ignored Build Step** → добавьте:
   ```bash
   exit 1
   ```
   (это заставит Vercel пропускать все автоматические билды)

Теперь Vercel будет деплоить **ТОЛЬКО** когда вы вручную запустите `vercel --prod`.

---

## ✅ Итоговый Workflow (рекомендуемый)

```
╔════════════════════════════════════════════════╗
║ Sandbox (AI Developer)                        ║
║ - Разработка                                   ║
║ - Тестирование                                 ║
║ - Любые эксперименты                          ║
╚═══════════════════╤════════════════════════════╝
                    │
                    │ git push (любые коммиты)
                    ▼
╔════════════════════════════════════════════════╗
║ GitHub                                         ║
║ - Все коммиты (dev, WIP, fixes, etc.)        ║
║ - История изменений                            ║
║ - Backup кода                                  ║
║ - Collaboration                                ║
╚═══════════════════╤════════════════════════════╝
                    │
                    │ РУЧНОЙ деплой
                    │ vercel --prod
                    │ (только когда готово)
                    ▼
╔════════════════════════════════════════════════╗
║ Vercel Production                              ║
║ - https://virtry.vercel.app                   ║
║ - Только финальные, протестированные версии   ║
║ - Стабильная Production среда                 ║
╚════════════════════════════════════════════════╝
```

---

## 💡 Best Practices

### 1. Коммитите часто в GitHub

```bash
# ✅ Хорошо: частые коммиты
git commit -m "Add button"
git commit -m "Fix alignment"
git commit -m "Update colors"
git push origin main
```

GitHub - это ваш backup и история. Коммитьте всё!

### 2. Тестируйте перед Production деплоем

```bash
# Всегда проверяйте локально:
npm run build
pm2 restart webapp
curl http://localhost:3000

# Если всё OK, тогда:
vercel --prod
```

### 3. Используйте понятные commit messages для релизов

```bash
# ✅ Хорошо:
git commit -m "Release v1.0.1: Add virtual try-on feature"

# ❌ Плохо:
git commit -m "update"
```

### 4. Держите список изменений

Создайте файл `CHANGELOG.md`:

```markdown
# Changelog

## v1.0.1 (2026-02-05)
- Add virtual try-on feature
- Fix error handling
- Update UI animations

## v1.0.0 (2026-02-04)
- Initial release
```

### 5. Проверяйте Production после деплоя

```bash
# После vercel --prod:
curl https://virtry.vercel.app
curl https://virtry.vercel.app/api/tryon

# Откройте в браузере и протестируйте
```

---

## 🐛 Troubleshooting

### Q: Забыл настроить Environment Variables

```bash
# Добавьте их сейчас:
vercel env add GATEWAY_URL production
vercel env add GATEWAY_TOKEN production

# Redeploy:
vercel --prod
```

### Q: Vercel деплоит автоматически, но я этого не хотел

```bash
# Отключите Git integration:
# Vercel Dashboard → Settings → Git → Disconnect
```

### Q: Хочу откатить Production на предыдущую версию

```bash
# Vercel Dashboard → Deployments
# Найдите нужную версию → ⋯ → Promote to Production
```

### Q: Как посмотреть что сейчас на Production?

```bash
vercel inspect --prod
```

---

## 📚 Полезные команды Vercel CLI

```bash
# Войти в аккаунт
vercel login

# Деплой на preview
vercel

# Деплой на production
vercel --prod

# Список деплоев
vercel ls

# Информация о проекте
vercel inspect

# Логи
vercel logs

# Откат (promote старую версию)
# Только через Dashboard

# Добавить env variable
vercel env add VAR_NAME production

# Посмотреть env variables
vercel env ls

# Удалить env variable
vercel env rm VAR_NAME production

# Удалить проект
vercel remove
```

---

## ✅ Итог

### Ваш Workflow:

1. **Sandbox** - работаете здесь, любые изменения
2. **GitHub** - `git push` после каждой сессии (backup + история)
3. **Vercel** - `vercel --prod` только когда финальная версия готова

### Преимущества:

✅ **Полный контроль** - деплоите когда решите  
✅ **GitHub для всего** - все коммиты сохраняются  
✅ **Безопасно** - случайно не задеплоите сырой код  
✅ **Просто** - нет сложных веток и настроек

---

## 🚀 Следующие шаги

### Прямо сейчас:

1. Установите Vercel CLI:
   ```bash
   npm install -g vercel
   ```

2. Войдите в аккаунт:
   ```bash
   vercel login
   ```

3. Первый деплой:
   ```bash
   cd /home/user/webapp
   vercel
   ```

4. Настройте Environment Variables:
   ```bash
   vercel env add GATEWAY_URL production
   vercel env add GATEWAY_TOKEN production
   ```

5. Production deploy:
   ```bash
   vercel --prod
   ```

**Готово!** Теперь у вас:
- ✅ Sandbox для разработки
- ✅ GitHub для всех коммитов
- ✅ Vercel для финальных релизов

🎉 Идеальный workflow!
